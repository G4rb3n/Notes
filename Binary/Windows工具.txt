windbg可以通过双机调试（配合VM），达到调试内核的目的
svchost.exe是一类通用进程的名称，是和运行动态链接库的windows系统服务相关的，
windows启动时，svchost会检查注册表中的服务，运行并载入它们，所以同时有多个svchost.exe在运行时正常的，
每一个都表示系统运行的一类基本服务，病毒也经常伪装成svchost

windows有一个特殊的进程Idle，当CPU运行此进程时相当于在休息

用户态的地址都是低2G（小于0x80000000）的，而一般代码段（.text）位于用户空间顶部，
所以平常所看到的系统函数地址为0x7???????

s -u 0 L100 "当年在交大"：从地址0开始的100范围内查找unicode值"当年在交大"
dd/db 地址：将地址内存以dwords/bytes的方式展示出来
eb 地址 xx xx：向地址写入字节
.writenmem 文件名 地址 范围：将内存写入文件

kPL：查看当前的函数
lm：查看进程加载了哪些模块
lmm 模块名 v：查看模块名的详细信息

!address 地址
查看对应内存页的属性

!process 0 0 进程名：查看当前进程信息（内核态）
~0s：切换到0号线程
|：显示当前进程的情况
k/kn 100：查看函数的调用链（栈回溯）
kv：获得当前函数的参数的
 
地址
dt：观察指定结构体的内容
?? （unsigned int）（x）：将x转换成无符号整形
!error：显示错误码的内容

!process 地址 2：显示进程的所有线程
!thread 地址：显示线程的函数链

.dump：存储内存映像
!locks：列出进程锁
!cs -l：列出关键区

!analize -v：自动从转储文件中分析出问题

u .
u $ip 
上面的两个命令是效果是一样的, 反汇编当前$ip地址上的8条命令

uf .
uf $ip 
上面两个命令的效果是一样的, 反汇编当前$ip地址上的整个函数

ub .
ub $ip 
反汇编$ip之前的8条指令

ub $ip L2a
反汇编$ip地址之前的42条指令，L范围

u $ip $ip+a 
反汇编地址$ip到地址$ip+10之间的指令. 注意, 这里包括$ip, 不包括$ip+10

sxe ld:模块名	加载模块

!gflag + ?		堆调试
		htc - 堆尾检查		*
		hfc - 堆释放检查		*
		hpc - 堆参数检查		*
		ust - 用户态栈回溯
		htg - 堆标志
		hvc - 调用时验证
		hpa - 启用堆页		*

